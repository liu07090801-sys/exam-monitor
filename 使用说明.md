# 屏幕监控考试系统 — 使用说明

> 版本：v1.0 ｜ 最后更新：2026-08-20
> 适用：局域网内单考场屏幕监考。监考端集中查看所有考生屏幕；考试端强制考生在浏览器中**全屏**停留在**洛谷考试网址**，切换窗口、退出网址、未全屏均自动**报警并截图取证**。

---

## 目录

1. [系统简介](#1-系统简介)
2. [角色说明](#2-角色说明)
3. [环境要求](#3-环境要求)
4. [安装部署](#4-安装部署)
5. [配置说明](#5-配置说明)
6. [考前准备](#6-考前准备)
7. [考试期间：监考操作](#7-考试期间监考操作)
8. [报警类型与处理](#8-报警类型与处理)
9. [考试结束](#9-考试结束)
10. [证据与数据](#10-证据与数据)
11. [常见问题](#11-常见问题)
12. [快速检查清单](#12-快速检查清单)

---

## 1. 系统简介

系统由两个程序组成：

| 程序 | 运行位置 | 作用 |
|------|---------|------|
| `server.py` | 监考老师电脑 | 仪表盘：实时接收截图、报警、心跳，集中展示所有考生状态 |
| `client.py` | 每台考生电脑 | 截屏上传 + 窗口/网址/全屏检测，违规即报警并取证 |

**核心强制规则（考试网址+全屏）**：考生浏览器（Chrome/Edge/Firefox 等）必须同时满足——

1. 处于 **F11 全屏** 状态；
2. 地址栏 URL 在考试网址白名单内（默认洛谷 `luogu.com.cn`）。

切换窗口、退出考试网址、退出全屏，任一发生即判违规：**先截图取证（本地存档 + 上传监考端），再弹出全屏警示**；监考端同步显示红色报警并记录日志。

**工作流程一句话**：监考老师先启动 `server.py` → 考生依次启动 `client.py` → 考生在宽限期内打开洛谷考试页并 F11 全屏 → 考试期间监考端实时监视。

---

## 2. 角色说明

| 角色 | 负责内容 |
|------|---------|
| **监考老师** | 启动监考端、观察仪表盘、处理报警、结束监考、导出日志 |
| **考生** | 启动客户端后，在宽限期内打开洛谷考试网址并全屏，之后正常考试 |
| **技术员** | 部署程序、修改配置（token、白名单、宽限期）、测试联调 |

---

## 3. 环境要求

| 项目 | 要求 |
|------|------|
| 操作系统 | 考试端必须 Windows 10/11（依赖 Win32 获取窗口/全屏信息）；监考端 Windows/Linux/macOS 均可 |
| Python | 3.10+ |
| 网络 | 两端同一局域网，监考端 **8765 端口** 对考生机可达（防火墙需放行） |
| 浏览器 | 考生使用 Chrome / Edge / Firefox / 360 浏览器（进程名在 `exam.browsers` 中可配） |

---

## 4. 安装部署

### 4.1 监考端

```bash
pip install -r requirements.txt
```

### 4.2 考试端

```bash
pip install -r requirements.txt
pip install uiautomation   # 强烈推荐：启用"地址栏 URL 级"网址检测
```

> **注意**：`uiautomation` 用于读取浏览器地址栏真实 URL。未安装时系统自动降级为"窗口标题匹配"——只要页面标题含"洛谷"即视为合规，**不能**精确校验网址，且此时 `exam.strict_url_check: true` 会直接判违规。正式考试请务必在考试端安装。

### 4.3 考生端整合包（免安装 Python，推荐考试机使用）

已打包好的文件夹 `考生端整合包/`，拷到考生电脑即可用，**无需安装 Python**：

```
考生端整合包/
├── 启动考生端.bat        # 双击启动，输入学号和监考端 IP 即可
├── client.exe            # 已打包的考生端程序（含全部依赖）
├── config.yaml           # 配置（与 exe 同目录，记事本可改）
├── 使用说明-考生端.txt     # 考生端简明说明
└── 使用说明.md            # 本完整手册副本
```

监考端部署脚本：`启动监考端.bat`（双击启动，等价于 `python server.py`）。

---

## 5. 配置说明

所有配置集中在程序目录下的 `config.yaml`，修改后**重启程序生效**。

### 5.1 必改项

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| `network.token` | `exam2024` | 认证令牌，**必须改为复杂随机字符串**，否则任何人可伪造考生端接入 |
| `exam.url_whitelist` | `luogu.com.cn` | 考试网址白名单，按需限定到具体比赛页（见 5.2） |

### 5.2 考试网址与全屏强制（exam）

```yaml
exam:
  enabled: true                # 是否启用强制（false=退回旧版标题白名单逻辑）
  url_whitelist:               # 白名单：域名，或 域名/路径 前缀
    - "luogu.com.cn"           # 整站放行
    # - "luogu.com.cn/contest/12345"   # 只放行某个比赛页（更严格）
  browsers:                    # 需要校验网址的浏览器进程
    - "chrome.exe"
    - "msedge.exe"
    - "firefox.exe"
    - "360se.exe"
    - "360chrome.exe"
  fullscreen_required: true    # 浏览器必须 F11 全屏
  grace_seconds: 30           # 启动宽限期（秒），考生打开浏览器进入考试页的时间
  strict_url_check: false      # true=读不到地址栏直接判违规（更严格，可能误报）
```

- **白名单匹配规则**：域名相等或为其子域（`www.luogu.com.cn` 匹配 `luogu.com.cn`）；带路径的条目按路径前缀匹配（`luogu.com.cn/contest` 不会误放行 `contest2`）；仿冒域名（如 `evil-luogu.com.cn.evil.com`）不会放行。
- **宽限期**：客户端启动后 `grace_seconds` 秒内任何窗口都不判违规，用于考生打开浏览器、登录并进入考试页。超时未就位将开始报警。按考场实际准备时间调整。
- **strict_url_check**：地址栏读取失败时（未装 uiautomation、浏览器重启中等），`false` 退回标题匹配、`true` 直接判违规。

### 5.3 客户端锁定模式（client）

```yaml
client:
  lock_mode: "popup"           # popup=违规时才弹出警示界面（推荐）
                               # overlay=全程全屏锁定（旧行为）
  alert_hold_seconds: 0        # 违规警示保持秒数，0=保持到考生恢复合规
```

| 模式 | 行为 | 适用 |
|------|------|------|
| `popup`（默认，推荐） | 监控窗口平时隐藏，**不遮挡考试画面、不会出现在截图里**；违规时弹出全屏红色警示，考生恢复合规后自动收起；断线时显示角落小窗提示连接状态 | 绝大多数场景 |
| `overlay` | 全程全屏锁定窗口，拦截 Alt+F4/Escape/Ctrl+W | 必须全程锁屏的场景；**注意：截图内容会一直是锁定界面，看不到考生屏幕** |

### 5.4 监考端（server）

```yaml
server:
  client_timeout: 15           # 超过此秒数无心跳判掉线
  freeze_timeout: 90           # 在线但超过此秒数无截图帧判"画面冻结"
  alert_hold_seconds: 300      # 卡片红灯保持秒数，超时自动恢复绿色
  alert_flash_duration: 3000   # 报警横幅显示时长（毫秒）
  max_screenshot_history: 200  # 每考生内存截图历史条数（翻页浏览用）
  max_screenshots_disk: 0      # 落盘截图总量上限，0=不限制（仅按保留天数清理）
  teacher_password: ""         # 非空时「结束监考/导出日志」需输入此口令
```

- **teacher_password**：留空 = 不校验（原行为）。建议考前设置口令，防止误触结束监考或随意导出日志。口令明文存于配置文件，仅作防误触用。
- **max_screenshots_disk**：如设置 50000，超出后自动删除最旧截图记录和文件。

### 5.5 其他常用项

| 配置项 | 默认 | 说明 |
|--------|------|------|
| `screenshot.interval` | 5 | 截屏采样间隔（秒） |
| `screenshot.quality` | 60 | JPEG 质量（1-100，越小越省带宽） |
| `hash.send_threshold` | 24 | 画面变化大于此值才上传（调低更敏感） |
| `hash.force_send_interval` | 30 | 无论是否变化，每 N 秒强制上传一张（反冻结） |
| `window.whitelist` | `["洛谷"]` | 非浏览器窗口的标题白名单 |
| `window.violation_confirm_count` | 3 | 连续违规 N 次才报警（过滤误触弹窗） |

---

## 6. 考前准备

**第一步：技术员部署**

1. 监考端、考试端分别安装依赖（见第 4 节），考试端额外安装 `uiautomation`；
2. 修改 `config.yaml`：更换 `token`，配置 `exam.url_whitelist`（建议限定到具体比赛页）、`grace_seconds`、`teacher_password`；
3. 把同一份 `config.yaml` 复制到所有机器（保证 token 一致）；
4. 在监考端确认防火墙放行 8765 端口。

**第二步：启动监考端**

双击 `启动监考端.bat`（或命令行 `python server.py`），看到"监考中心"仪表盘即启动成功。

> 首次启动如弹出 Windows 防火墙提示，请点「允许访问」。若未弹出且考生端连不上，请以管理员身份运行：
> `netsh advfirewall firewall add rule name="考试监控8765" dir=in action=allow protocol=TCP localport=8765`

**第三步：启动考试端**

- **整合包方式（推荐）**：考生机双击 `启动考生端.bat`，按提示输入学号和监考端 IP；
- **源码方式**：`python client.py --student-id 2024001 --server ws://192.168.1.100:8765`（`--student-id` 学号必填，`--server` 默认本机）。

**第四步：考生就位**

考生端启动后进入宽限期（默认 30 秒），此时要求考生：

1. 打开 Chrome / Edge / Firefox；
2. 进入洛谷考试网址并登录；
3. 按 **F11 全屏**。

宽限期结束后系统开始强制检测。考生端平时**没有可见窗口**（popup 模式），属正常现象——只有违规、断线时才会弹出提示。

**第五步：监考老师确认**

- 所有考生卡片出现且状态灯为**绿色**；
- 点击考生卡片，右下角能看到该考生的最新截图（洛谷考试页画面）。

---

## 7. 考试期间：监考操作

### 7.1 仪表盘界面

| 区域 | 说明 |
|------|------|
| 顶部品牌栏 | 端口、认证/TLS 状态、实时时钟、在线/离线/报警统计徽章、导出与结束按钮 |
| 考生卡片 | 每位考生一张圆角卡片，自适应多列网格：**状态灯**（绿=正常 / 红=报警 / 灰=离线）+ 左侧状态色条、学号、主机名、截屏/报警徽章、最近违规摘要、最后心跳时间 |
| 事件日志 | 右侧滚动显示上线/离线/违规/冻结事件，彩色分级 + 斑马纹；顶部「全部/上线/违规/离线/系统」徽章可过滤 |
| 截图预览 | 点击考生卡片（或卡片上「预览」按钮），右下角显示其最新截图与实时刷新时间；**双击预览可弹出大图窗口** |
| 截图历史 | 卡片上「截图历史」按钮，两列网格翻页浏览该考生截图（每页 12 张，窗口打开期间新截图实时可见） |
| 底部状态条 | 服务监听地址、数据库与截图目录、保留天数 |

**状态灯说明**：报警后红灯保持 `alert_hold_seconds`（默认 300 秒），随后自动恢复绿色；考生持续违规会不断刷新红灯。

**界面主题**：全部配色读取自 config.yaml 的 `colors` 段（accent/border/hover 等扩展键可省略，省略时自动推导），改后重启即整体换肤。

### 7.2 日常操作

- **观察**：考试期间保持仪表盘可见，留意红灯、掉线、冻结事件；
- **抽查**：点卡片看最新截图，或「查看截图」翻看历史，确认考生画面确实是洛谷考试页；
- **处理**：出现报警时按第 8 节流程处理，结合证据截图判断是否作弊。

---

## 8. 报警类型与处理

### 8.1 违规报警（考生行为）

| 报警文案 | 含义 | 建议处理 |
|---------|------|---------|
| 切屏 -> 某窗口 | 前台切到了白名单外的程序（浏览器以外） | 结合证据截图判断：是否切出考试页 |
| 浏览器未全屏 | 浏览器退出了 F11 全屏 | 提醒考生重新全屏；反复出现需关注 |
| 考试网址不符 | 浏览器地址栏 URL 不在白名单（换网页/搜索） | **重点核查**：看日志里的实际网址和截图 |
| 无法读取浏览器网址 | 严格模式下地址栏读不到 | 可能浏览器重启中，或未装 uiautomation |
| 无法读取网址且标题不在白名单 | 非严格模式下网址读不到且标题不含"洛谷" | 结合截图判断 |

每次违规确认（连续 `violation_confirm_count` 次，默认 3 次）后：

1. 客户端**立即截图取证**（本地 + 上传，哈希存证），取证画面不含警示遮罩；
2. 客户端弹出全屏红色警示，**考生恢复合规后自动收起**；
3. 监考端日志、横幅、卡片同步报警，卡片红灯亮起。

### 8.2 状态报警（连接异常）

| 报警文案 | 含义 | 建议处理 |
|---------|------|---------|
| 考生掉线: 学号 | 心跳超时（默认 15 秒） | 检查考生机网络/客户端是否被杀 |
| 画面冻结：已 N 秒未收到截图 | 在线但长时间无截图帧 | 结合截图判断：可能客户端卡死、考生用虚拟桌面等 |
| 状态灯灰色 | 离线 | 同上 |

> **静止画面不会触发冻结报警**：画面静止时客户端仍会按 `force_send_interval`（默认 30 秒）定期补帧，属正常。

---

## 9. 考试结束

1. 监考老师点击 **「结束监考」**（配置了口令需先验证），所有在线考生端自动退出；
2. 点击 **「导出日志」** 生成本次考试完整事件记录 CSV（保存到 `exports/` 目录，UTF-8 带 BOM，Excel 可直接打开）；
3. 关闭监考端窗口即可退出。

> 报警记录**无法撤销**，会永久保存在数据库与导出日志中（连续违规确认机制已尽量减少误报）。

---

## 10. 证据与数据

### 10.1 截图目录

```
screenshots/
├── 2024001/                # 考生学号
│   ├── 2026-08-20/         # 日期
│   │   ├── 091234_123456.jpg
│   │   └── ...
└── 2024002/
```

- 客户端本地存档 + 监考端各存一份；
- 文件名含微秒，同秒多帧不覆盖；
- 违规取证的截图与常规截图混存，配合日志时间戳可定位证据。

### 10.2 数据库与日志

| 文件 | 内容 |
|------|------|
| `monitor.db` | SQLite：`events` 事件表（上线/违规/冻结/掉线）、`screenshots` 截图索引（含感知哈希存证） |
| `exports/monitor_log_日期_时间.csv` | 导出的完整事件日志 |
| 控制台日志 | 程序运行输出，含连接/清理等细节 |

### 10.3 自动清理

- 超过 `screenshot.retention_days`（默认 7 天）的记录与文件按 `cleanup_interval`（默认 1 小时）周期自动清理；
- `server.max_screenshots_disk` 设置后可限制落盘截图总量，超出删最旧。

---

## 11. 常见问题

**Q1：考生端会不会把自己窗口当成切屏报警？**
不会。客户端按窗口句柄豁免自身窗口；popup 警示弹出/恢复过程不会自我报警。

**Q2：监考端截图一直是锁定界面，看不到考生屏幕？**
客户端用了 `lock_mode: overlay`（旧行为）。改为 `popup`（默认）即可：窗口平时隐藏，只有违规时才弹出。

**Q3：考生切到别的网页了但没报警？**
考试端未安装 `uiautomation`（网址检测降级为标题匹配）。安装 `pip install uiautomation`，或把 `exam.strict_url_check` 设为 `true`。

**Q4：误报太多怎么办？**
按顺序排查：①调大 `exam.grace_seconds`（考生没来得及就位）；②调大 `window.violation_confirm_count`（弹窗闪烁）；③把考试必需的其他程序加入 `window.whitelist` / `whitelist_processes`；④确认白名单写法（见 5.2 匹配规则）。

**Q5：想只允许洛谷某个比赛页，怎么写白名单？**
```yaml
exam:
  url_whitelist:
    - "luogu.com.cn/contest/12345"
```
该写法只放行 `https://www.luogu.com.cn/contest/12345` 及其子路径（如 `.../12345/submit`）。

**Q6：提示"认证失败：令牌不正确"？**
监考端与考试端的 `config.yaml` 中 `network.token` 不一致。统一后重启两端。

**Q7：忘记 teacher_password 怎么办？**
口令明文存在监考端 `config.yaml` 的 `server.teacher_password` 中，打开即可查看/修改（本口令仅防误触，非安全认证）。

**Q8：考生端连不上监考端？**
①监考端是否已启动；②防火墙是否放行 8765 端口；③IP 地址是否正确（`--server` 参数）；④用 `ping` 测试连通性。

**Q9：多个考场怎么隔离？**
当前为单考场设计。可给不同考场配置不同 `network.port`，各自运行独立的 `server.py`。

**Q10：考生退出 F11 全屏后警示一直不消失？**
考生恢复"全屏 + 考试网址"后警示自动收起（`alert_hold_seconds: 0` 时保持到恢复合规为止）；若配置了大于 0 的保持秒数，则按秒数自动收起。

---

## 12. 快速检查清单

### 考前

- [ ] 两端依赖安装完成，考试端已装 `uiautomation`
- [ ] `token` 已更换且两端一致
- [ ] `url_whitelist` 已按考场要求配置（建议限定比赛页）
- [ ] `grace_seconds` 已按准备时间调整
- [ ] `teacher_password` 已设置（可选）
- [ ] 监考端启动，端口 8765 可达
- [ ] 试连一台考试端：卡片变绿、能看到洛谷页面截图

### 考试期间

- [ ] 所有考生卡片绿灯在线
- [ ] 出现报警时：看日志实际网址 → 看证据截图 → 处理并记录
- [ ] 定期抽查考生历史截图

### 考试结束

- [ ] 点击「结束监考」关闭全部考生端
- [ ] 导出日志 CSV 存档
- [ ] 核对 `screenshots/` 与 `monitor.db` 留存

---

## 附：命令行参数

```bash
# 监考端（无参数）
python server.py

# 考试端
python client.py --student-id <学号> [--server ws://<监考端IP>:8765]

# 示例
python client.py --student-id 2024001                                   # 默认连本机
python client.py --student-id 2024001 --server ws://192.168.1.100:8765  # 连真实监考端
```

---

*使用说明 v1.0 ｜ 最后更新：2026-08-20*
